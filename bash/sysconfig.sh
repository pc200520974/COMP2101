# TODO: Add a loop that iterates through each line of the input file, extract the necessary information, and print it.
while IFS= read -r line; do
  # Process each line and extract necessary information
  # Print the extracted information
done < "$input_file"
